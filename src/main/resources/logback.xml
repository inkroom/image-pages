<?xml version="1.0" encoding="UTF-8"?>
<!--scan:
            当此属性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true。
scanPeriod:
            设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。
debug:
            当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。
configuration 子节点为 appender、logger、root
            -->
<configuration scan="false" scanPeriod="60 seconds" debug="false">
    <!--用于区分不同应用程序的记录-->
    <contextName>image-download</contextName>

<!--    <springProperty scope="context" name="URL_LOG_NAME"-->
<!--                    source="logging.url.name"-->
<!--    />-->

<!--    <springProperty scope="context" name="LOG_HOME" source="logging.path"/>-->
    <!-- 日志保存时间 -->
    <property name="LOG_HOME" value="logs"/>


    <!--控制台-->
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度 %logger输出日志的logger名 %msg：日志消息，%n是换行符 -->
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] %-5level %logger : %msg%n</pattern>
            <!--解决乱码问题-->
            <charset>UTF-8</charset>
        </encoder>
    </appender>


    <!--前台错误日志记录输出-->
    <appender name="errorFile" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_HOME}/error.log</file>
        <append>true</append>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] %-5level %logger : %msg%n</pattern>
        </encoder>
        <filter class="cn.inkroom.image.download.ErrorLogFilter">
        </filter>
    </appender>

    <logger name="org.apache.http" level="DEBUG" additivity="false">
        <appender-ref ref="stdout"/>
    </logger>

    <logger name="cn.inkroom.image.download.DownloadThread" level="DEBUG" additivity="false">
        <appender-ref ref="errorFile"/>
        <appender-ref ref="stdout"/>
    </logger>

    <root level="DEBUG">
        <appender-ref ref="stdout"/>
    </root>
</configuration>